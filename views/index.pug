extends layout
block content
  .container.py-5
    h1.text-center.mb-5 #{title}
    .row.g-4
      each servicio in servicios
        .col-md-4
          .card.h-100
            .card-body
              h3.card-title #{servicio.nombre}
              p.card-text #{servicio.descripcion}
